@functions {
    string GetCategoryIcon(string categoryName)
    {
        return categoryName.ToLower() switch
        {
            "may mặc" => "apparel",
            "mỹ phẩm" => "face",
            "điện tử" => "devices",
            "hàng gia dụng" => "home",
            "mẹ và em bé" => "child_care",
            "xe máy" => "motorcycle",
            "oto" => "directions_car",
            "đồ chơi - phụ kiện" => "toys",
            "bàn ghế - nội thất" => "chair",
            _ => "category"
        };
    }
}

<!-- Sidebar (25%) -->
<aside class="w-full lg:w-1/4 space-y-12">
    <div>
        <h2 class="text-sm font-black uppercase tracking-[0.2em] text-slate-900 mb-8">Các loại hàng</h2>
        <nav class="space-y-1">
            <a href="javascript:;" onclick="doSearch(1, 0)" data-category-id="0" class="category-link active group relative flex items-center gap-3 px-4 py-3 rounded-lg text-slate-600 
                hover:bg-indigo-50 hover:text-indigo-700 
                transition-all duration-200 [&.active]:bg-indigo-50 [&.active]:text-indigo-700">
                <span
                class="absolute left-0 top-1/2 -translate-y-1/2 w-1 h-6 rounded-r bg-indigo-600 opacity-0 group-[.active]:opacity-100 transition"></span>
                <span
                class="material-symbols-outlined text-xl text-slate-400 group-hover:text-indigo-600 group-[.active]:text-indigo-600 transition">list</span>
                <span class="text-sm font-medium tracking-wide">Tất cả sản phẩm</span>
            </a>

            @foreach (var category in ViewBag.Categories)
            {
                <a href="javascript:;" onclick="doSearch(1, @category.CategoryID, '@category.CategoryName')"
                       data-category-id="@category.CategoryID" class="category-link group relative flex items-center gap-3 px-4 py-3 rounded-lg text-slate-600 
                    hover:bg-indigo-50 hover:text-indigo-700 
                    transition-all duration-200 [&.active]:bg-indigo-50 [&.active]:text-indigo-700">
                    <!-- Active indicator (left bar) -->
                    <span
                    class="absolute left-0 top-1/2 -translate-y-1/2 w-1 h-6 rounded-r bg-indigo-600 opacity-0 group-hover:opacity-100 group-[.active]:opacity-100 transition"></span>

                    <!-- Icon -->
                    <span class="material-symbols-outlined text-xl text-slate-400 
                        group-hover:text-indigo-600 group-[.active]:text-indigo-600 transition">
                        @GetCategoryIcon(category.CategoryName)
                    </span>

                    <!-- Text -->
                    <span class="text-sm font-medium tracking-wide">
                        @category.CategoryName
                    </span>

                </a>
            }
        </nav>
    </div>

    <div>
        <h2 class="text-sm font-black uppercase tracking-[0.2em] text-slate-900 mb-8">Khoảng giá (VNĐ)</h2>
        <div class="space-y-4">
            <div class="flex flex-col gap-2">
                <label class="text-[10px] font-bold text-slate-400 uppercase tracking-tighter">Từ</label>
                <div class="relative">
                    <input type="number" id="min-price-input" placeholder="0"
                           class="w-full pl-4 pr-12 py-3 bg-gray-50 border-none rounded-lg text-sm font-semibold text-slate-700 focus:ring-2 focus:ring-indigo-500 transition-all">
                    <span
                    class="absolute right-4 top-1/2 -translate-y-1/2 text-[10px] font-bold text-slate-400">đ</span>
                </div>
            </div>
            <div class="flex flex-col gap-2">
                <label class="text-[10px] font-bold text-slate-400 uppercase tracking-tighter">Đến</label>
                <div class="relative">
                    <input type="number" id="max-price-input" placeholder="2.500.000"
                           class="w-full pl-4 pr-12 py-3 bg-gray-50 border-none rounded-lg text-sm font-semibold text-slate-700 focus:ring-2 focus:ring-indigo-500 transition-all">
                    <span
                    class="absolute right-4 top-1/2 -translate-y-1/2 text-[10px] font-bold text-slate-400">đ</span>
                </div>
            </div>
            <button onclick="doSearch(1)"
                class="w-full py-4 bg-indigo-600 hover:bg-indigo-700 rounded-lg text-xs font-bold uppercase tracking-widest text-white transition-all shadow-lg shadow-indigo-100">
                Lọc sản phẩm
            </button>
        </div>
    </div>

    <div class="pt-8 border-t border-gray-100">
        <h3 class="text-[10px] font-black uppercase tracking-[0.2em] text-slate-400 mb-4">Trợ giúp mua sắm</h3>
        <button
            class="w-full py-4 border-2 border-gray-100 rounded-sm text-xs font-bold uppercase tracking-widest text-slate-500 hover:bg-gray-50 hover:border-gray-200 transition-all">
            Hướng dẫn chọn size
        </button>
    </div>
</aside>
